{
  "name": "fluent-ffmpeg-multistream",
  "version": "1.0.0",
  "description": "Adds support for multiple stream inputs/outputs to fluent-ffmpeg.",
  "main": "src/index.js",
  "directories": {
    "test": "test"
  },
  "repository": {	
    "type": "git",
    "url": "https://github.com/t-mullen/fluent-ffmpeg-multistream.git"
  },
  "scripts": {
    "test": "standard --fix src/** && tape test/*.js"
  },
  "author": "Thomas Mullen",
  "license": "MIT",
  "devDependencies": {
    "fluent-ffmpeg": "^2.1.2",
    "standard": "^8.6.0",
    "tape": "^4.6.3"
  }
}
